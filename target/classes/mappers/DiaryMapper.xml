<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.todolist.mapper.DiaryMapper">
  	<select id="selectNow" resultType="string">
  		select now()
  	</select>
  	
<!-- 리스트 등록 쿼리문 -->
  	<insert id="insert">
  		insert into kyj_todolist (title, dueDate , writer) values(#{title}, #{dueDate} , #{writer})
  	</insert>
  	
<!--   	리스트 전체 조회 쿼리문 -->
  	<select id="selectAllList" resultType="com.todolist.domain.DiaryVO">
  		select * from kyj_todolist order by dno desc
  	</select>
  	
<!--  만료일 수정 쿼리문 -->
  	<update id="updateFinished">
  		update kyj_todolist set finished = #{finished} where dno = #{dno}
  	</update>
  	
<!--   	수정 쿼리문 -->
  	<update id="updateDiary">
  		update kyj_todolist set title = #{title}, dueDate = #{dueDate} where dno = #{dno}
  	</update>
  	
<!--   	아이디로 조회 쿼리문 -->
  	<select id="selectAllListById" resultType="DiaryVO">
  		select * from kyj_todolist where writer = #{memberId} order by dno desc
  	</select>
  	
<!--   	내일이 만료일인 리스트 조회 쿼리문 -->
  	<select id="selectDiaryDueTomorrow" resultType="DiaryVO">
  		select * from kyj_todolist where dueDate = date_add(curdate(), interval 1 day)
  	</select>
  	
<!--   	리스트 삭제 쿼리문 -->
  	<delete id="deleteDiary" >
    	delete from kyj_todolist where dno = #{dno}
	</delete>
  	
  	
<!--   	검색 -->
<sql id="search">
	<where>
		<if test="writer != null and writer != ''">
			writer = #{writer}
		</if>
		
		<if test="searchTypes != null and searchTypes == 'title' and searchWord != null and searchWord != ''">
			<trim prefix="AND">
				title like concat('%', #{searchWord} ,'%')
			</trim>
		</if>
		
		<if test="finished != null and finished != '' ">
			<trim prefix="AND">
				finished = ${finished}
			</trim>
		</if>
		
		<if test="from != null and from != '' and to != null and to != '' ">
			<trim prefix="AND">
			duedate between #{from} and #{to}
			</trim>
			
		</if>
			<if test="from != null and from != '' and (to == null or to == '')">
			<trim prefix="AND">
				duedate &gt;= #{from}
			</trim>
		</if>

		<if test="to != null and to != '' and (from == null or from == '')">
			<trim prefix="AND">
				duedate &lt;= #{to}
			</trim>
		</if>
	</where>
</sql>

	<select id="selectSearchList" resultType="DiaryVO">
		select * from kyj_todolist
		<include refid="search"></include>
		order by dno desc
	</select>
  	
</mapper>